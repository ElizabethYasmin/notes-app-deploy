[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: any path that isn't a real file resolves to index.html,
# so client-side routes (e.g. /notes, /login) work on direct load/refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
